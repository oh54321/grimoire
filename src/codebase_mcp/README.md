# codebase_mcp

The implementation of the **Grimoire** MCP server. See the [project README](../../README.md) for usage, configuration, and the full tool reference.

Run: `grimoire` (installed console script) or `python -m codebase_mcp`.
